#include <stdio.h>
/** 
 * main - prints the phrase with proper grammar, but the outcome is a piece of art,
 * Return: 0 exits successfully non zero Otherwise
 */
int main(void)
{
	
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
