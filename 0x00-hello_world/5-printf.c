#include <stdio.h>
/**
 * main - prints with printf function,
 * Return: 0 exits successfully non zero Otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
