#include <stdio.h>
/**
 * main - Entry point
 * Return: 0 if exited successfully non zero OW
**/

int main(void)
{
	char x;

	for (x = 'a'; x <= 'z'; x++)
		putchar (x);
		putchar ('\n');
	return (0);
}
